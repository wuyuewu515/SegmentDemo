<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    android:color="@color/btn_disable">
    <corners
        android:radius="20dp" />
    <stroke
        android:width="0.5dp"
        android:color="@color/devider_gray" />
    <solid
        android:color="@color/bg_white" />
</shape>